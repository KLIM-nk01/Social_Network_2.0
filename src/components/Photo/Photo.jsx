import React from 'react';
import PhotoStyle from './Photo.module.scss';

const Photo = (props) => {
    return (
        <div className={PhotoStyle.photo_wrapper}>
ertertert
        </div>
    );
};

export default Photo;