import React from 'react';
import ChatStyle from './Chat.module.scss';

const Chat = (props) => {
    return (
        <div className={ChatStyle.chat_wrapper}>
            Chat
        </div>
    );
};

export default Chat;