import React from 'react';
import DialogStyle from './Dialog.module.scss';

const Dialog = (props) => {
    return (
        <div className={DialogStyle.dialog_wrapper}>
            Dialogs
        </div>
    );
};

export default Dialog;